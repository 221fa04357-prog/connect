{
  "buildCommand": "npm install --prefix replica --legacy-peer-deps && npm run build --prefix replica",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
